import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'barraprogresiva',
  webDir: 'dist/barraprogresiva',
  server: {
    androidScheme: 'https'
  }
};

export default config;
